# Frontend Notes

## CSS
- Location: `src/style.css`
- Defaults to light mode (white #ffffff bg, dark #213547 text)
- Dark mode via `prefers-color-scheme: dark` media query

## To Add
- Theme toggle (dark/light)